[install]
# Configurações de instalação
cache = true
production = false
dev = true
optional = true
peer = true

[run]
# Configurações de execução
bun = "bun"
node = "node"

[test]
# Configurações de teste
timeout = 5000
coverage = true

[install.lockfile]
# Configurações do lockfile
save = true
saveDev = true
saveOptional = true
savePeer = true
